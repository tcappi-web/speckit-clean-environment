# ============================================================
# Clean Environment — .env.example
# Gerado por /speckit.security-setup com base no security-context.md
# ============================================================
# ⚠️  NÃO COLOQUE VALORES REAIS AQUI — este arquivo é commitado
# ⚠️  Copie para .env.local (gitignored) e preencha os valores
# ============================================================

# ============================================
# PUBLIC — Frontend (visível no bundle)
# ⚠️  NUNCA coloque secrets em variáveis com prefixo público
# ============================================
# VITE_*           — Vite (se aplicável)
# NEXT_PUBLIC_*    — Next.js (se aplicável)
# REACT_APP_*      — CRA (se aplicável)
# PUBLIC_*         — SvelteKit/Astro

VITE_API_URL=http://localhost:3001
VITE_APP_NAME=
VITE_APP_VERSION=1.0.0

# ============================================
# BACKEND ONLY — Server-side (seguro para secrets)
# ============================================

# --- Database ---
DATABASE_URL=postgresql://user:password@localhost:5432/dbname
DATABASE_POOL_MIN=2
DATABASE_POOL_MAX=10
DATABASE_SSL=true

# --- Authentication ---
# Gere com: openssl rand -hex 32
JWT_SECRET=
REFRESH_TOKEN_SECRET=
JWT_EXPIRATION=15m
REFRESH_TOKEN_EXPIRATION=7d

# --- Session ---
SESSION_SECRET=
COOKIE_DOMAIN=localhost
COOKIE_SECURE=true
COOKIE_SAMESITE=strict

# --- Encryption at-rest ---
ENCRYPTION_KEY=
ENCRYPTION_ALG=aes-256-gcm

# --- Bcrypt ---
BCRYPT_ROUNDS=12

# --- Third-Party APIs ---
# ANTHROPIC_API_KEY=
# OPENAI_API_KEY=
# STRIPE_SECRET_KEY=         # ⚠️ Se PCI-DSS ativo
# STRIPE_WEBHOOK_SECRET=

# --- Email ---
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=noreply@example.com

# --- OAuth ---
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=

# --- Storage ---
# S3_BUCKET=
# S3_REGION=
# AWS_ACCESS_KEY_ID=         # ⚠️ Preferir IAM role em produção
# AWS_SECRET_ACCESS_KEY=

# --- Redis ---
# REDIS_URL=redis://localhost:6379

# --- Monitoring ---
SENTRY_DSN=
LOG_LEVEL=info
NODE_ENV=development

# --- Rate Limiting ---
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# --- CORS ---
# Whitelist explícita (Constituição III.4) — separadas por vírgula
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
